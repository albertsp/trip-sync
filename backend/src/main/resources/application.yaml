spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/tripsync_data
    username: tripsync
    password: tripsync123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true